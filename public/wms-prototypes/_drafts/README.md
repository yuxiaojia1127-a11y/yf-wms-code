# WMS Prototype Drafts

这里用于存放 AI 新生成的原型草稿文件。

## 规则

- 这里的 HTML 默认都是草稿，不是正式入口文件。
- 不要把这里的文件直接挂到总入口按钮。
- 不要把这里的文件直接作为线上正式地址。
- 方案确认后，再人工合并到正式文件：
  - `02WEB端HTML版/WMS原型总入口.html`
  - `02WEB端HTML版/WMS-WEB端原型.html`
  - `03APP端HTML版/WMS-APP端原型.html`

